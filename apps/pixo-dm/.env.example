# The Pixl Slack bot token DMs are sent from (Pixo). Same bot as pixorpheus.
SLACK_BOT_TOKEN=
# Shared secret the dashboard sends as x-api-key. Must match the dashboard's
# EXTERNAL_API_KEY exactly.
EXTERNAL_API_KEY=
# Port to listen on. Railway injects this automatically.
PORT=4100
